class Solution {
public:
    int findMin(vector<int>& nums) {
        int n=nums.size();
        int ans;
        for(int i=0;i<n;i++){
            sort(nums.begin(), nums.end());
            ans=nums[0];
        }
        return ans;
    }
};
